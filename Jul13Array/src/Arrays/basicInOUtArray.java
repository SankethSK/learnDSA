package Arrays;
import java.util.*;
public class basicInOUtArray {
	public static void main(String[] args)
	{
		Scanner in=new Scanner(System.in);
		System.out.print("Enter the length of the Array:");
		int n=in.nextInt();
		int[] arr=new int[n];
		for(int i=0;i<arr.length;i++)
		{
			arr[i] = in.nextInt();
//			System.out.println(arr[i]);
		}
		f
		for(int i=0;i<arr.length;i++)
		{
//			arr[i] = in.nextInt();
			System.out.println(arr[i]);
		
		}	System.out.println("As a string"+Arrays.toString(arr));
		
		in.close();
		
	
	}
}
